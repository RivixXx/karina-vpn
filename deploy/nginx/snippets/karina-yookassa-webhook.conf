location = /webhooks/yookassa {
    proxy_pass http://127.0.0.1:8091/webhooks/yookassa;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-Proto $scheme;
    client_max_body_size 64k;
    access_log off;
}
